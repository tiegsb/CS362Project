/******RANDOMSTRING.c******

inputChar()
This function simply assigns a character
of random range from ASCII 32 - 125 to the 
randchar variable. 

inputString()
This function allocates memory for a string
the same size as the target string ("reset").
Then it creates a new string similar to the target
string, and iterates through a loop to assign a random
variable from the target string to the "random string".

*/

//end of randomstring.c