scoreFor(): Function has complete coverage.

getWinners(): line 470 is never run, because there are never unused players, the unittest2 always runs with 4 players.  It should be changed to run with a varied number of players.

isGameOver(): Function has complete coverage.

fullDeckCount(): Function has complete coverage.