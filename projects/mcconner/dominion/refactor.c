Rachael McConnell
CS362 Fall 2015
October 3, 2015
Assignment 2

Adventurer
-Moved adventurer switch statement into a separate function, adventurerCard
-Changed cardDrawn from copper, silver, or gold to copper, estate, or gold

Smithy
-Moved smithy switch statement into a separate function, smithyCard
-Changed for loop to only get 2 new cards instead of 3

Feast
-Moved feast card switch statement into a separate function, feastCard
-In updateCoins function, coins are only updated by 4 instead of 5.

CouncilRoom
-Moved councilRoom switch statement into a separate function, councilRoomCard

Steward
-Moved steward card switch statement into a separate function, stewardCard
-Switched the choices in the if statements, it was: if choice1 == 1, call drawCard twice, elseif choice1==2 increment coins by 2. Now, if choice1==1 increment coins by 2, if choice1==2, call drawCard twice
